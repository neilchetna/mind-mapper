Your task is to suggest nodes for a given graph. Its a mind map with nodes and child nodes.
Suggest nodes for a given target nodes. Here the Graph representation of the mind map:

"{{.Graph}}"

Return your response ONLY as a valid JSON object in the following format:
{
  "nodes": [
    { "text": "<string>" },
    { "text": "<string>" }
  ]
}

Do not include explanations, comments, or extra text. Return only valid JSON.