import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants
} from './button.svelte';

import ButtonLabel from './button-label.svelte';

export {
	Root,
	type ButtonProps as Props,
	//
	Root as Button,
	ButtonLabel,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
};
